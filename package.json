{
  "name": "pantry-firebase-baseline",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"vitest run\"",
    "seed:emulator": "node scripts/seed-emulator.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "firebase-tools": "^13.31.2",
    "vitest": "^2.1.8"
  }
}
